<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Opret nyt portefølje</title>
    <link href="/opretPortefolje.css" rel="stylesheet" />
</head>

<body>
   <!--container til opret portefølje formular-->
    <div class="opretPortefølje">
        <h1>Opret ny portefølje</h1>
        <form action="/portefolje/opret" method="POST"> 
           
           <div class="opretBox">
              <input type="text" placeholder="Portefølje navn" name="navn" required>
           </div>
           
           <div class="opretBox">
            <label for="kontoID">Vælg konto</label>
            <!--laver en drop down menu, så bruger kan bestemme hvilken konto de ønsker at tilknytte portefølje til. Vi henter konto vha. emredded JavaScript-->
            <select name="kontoID" id="kontoID" required>

              <!--foreach gennem hver konto (listearray) som er tilsendt fra serveren. For hver konto laves der en valgmulighed (option). Value bliver= konto.kontoID sendes tilbage til server, når bruger vælger-->
              <% konti.forEach(konto => { %>
                <option value="<%= konto.kontoID %>">
                  <%= konto.kontonavn %> (Saldo: <%= konto.saldo %>)
                </option>
              <% }); %>
            </select>
          </div>
    
           <button id="sendFormular" type="submit">Opret</button>
           <br>

        </form>
     </div>
</body>
</html>